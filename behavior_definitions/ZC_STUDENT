projection;
strict ( 2 );

define behavior for ZC_STUDENT //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action setAdmitted;
  use action unsetAdmitted;
  use action delete;
}
